/ {
    chosen {
        zephyr,console = &usb_serial;
		zephyr,shell-uart = &usb_serial;
        zephyr,settings-partition = &storage_partition;
        xecut,keypad = &uart0;
    };

};

&uart0 {
    status = "okay";
    current-speed = <9600>;
};

&usb_serial {
    status = "okay";
};

